import React from "react";
import styles from './index.less'
import { Spin, Form, Input, Icon, Button, message, Modal } from 'antd';

export default function IndexPage() {
  return (
    <div>
      <h1 className={styles.title}>Page index</h1>
      <Spin />
      <Button>aaaa</Button>
      <button>yrdty</button>
    </div>

  );
}
